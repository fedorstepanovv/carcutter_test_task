class AppConfig {
  AppConfig._();
  static final baseUrl = "https://dummy.restapiexample.com/api/v1";
  static const sendTimeout = Duration(seconds: 12);
  static const receiveTimeout = Duration(seconds: 12);
}
